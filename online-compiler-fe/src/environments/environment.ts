export const environment = {
  production: true,
  // Relative URL: in production the app is served by nginx (see nginx.conf.template),
  // which proxies /v1/* to the backend, so no absolute host is needed here.
  apiBaseUrl: ''
};
